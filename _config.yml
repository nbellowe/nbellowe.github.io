# Permalinks
#
# `relative_permalinks` was removed in Jekyll 3.0 and is now a hard build
# error; page permalinks resolve from the site source directory instead.
permalink:          "/:title"
highlighter:        pygments
timezone:           America/Denver

# Tagging settings
tag_title_prefix:   "Posts tagged #"
tag_title_suffix:   ""

# Do not copy these file to the output directory
exclude:            [ .gitignore, .gitmodules, CNAME, README.md, fractal/FractalExplorer/ ]

# Setup
title:               Nathan Bellowe
tagline:             My personal website.
url:                 "http://nbellowe.github.io"
paginate:            4

# Assets
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _sass
  style:            :compressed

# About/contact
author:
  name:              Nathan Bellowe
  email:             nbellowe@gmail.com

# Custom vars
version:             1.0.0
github:
  repo:              https://github.com/nbellowe/nbellowe

# Conversion
markdown:    redcarpet
excerpt_separator: "<!--break-->"

redcarpet:
  extensions: ["footnotes"]

kramdown:
  auto_ids: true

gems:
    - jekyll-redirect-from
    - jekyll-feed
    - jekyll-sitemap
